services:
  v79-tiquet-manager:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: v79-tiquet-manager
    ports:
      - "3050:3000"
    environment:
      - PORT=3000
    restart: unless-stopped
